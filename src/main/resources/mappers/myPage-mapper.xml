<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="myPageMapper">

	
	<select id="selectEncPw" parameterType="_int" resultType="string">
	SELECT MEMBER_PW FROM MEMBER WHERE MEMBER_NO = #{memberNo}
	</select>
	
	<update id="memberSecession">
	UPDATE MEMBER SET
	SECESSION_FL = 'Y'
	WHERE MEMBER_NO = #{memberNo}
	</update>
	
	
	<select id="pwCheck" resultType="string" parameterType="String">
	SELECT MEMBER_PW FROM MEMBER WHERE MEMBER_PW = #{memberPw}
	</select>
	





</mapper>
